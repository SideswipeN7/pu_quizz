<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:background="#0099cc"
             tools:context=".MainActivity">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->
    <LinearLayout android:id="@+id/fullscreen_content"
                  style="?metaButtonBarStyle"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:layout_gravity="bottom|center_horizontal"
                  android:background="@color/black_overlay"
                  android:orientation="vertical">
        <ImageView
                android:layout_width="match_parent"
                android:layout_height="57dp" app:srcCompat="@mipmap/logo" android:id="@+id/imageView"
                android:scaleType="center"/>
        <Button
                android:id="@+id/startGameBtn"
                android:layout_width="match_parent"
                android:layout_height="66dp"
                android:text="@string/new_game"
                android:layout_marginTop="15dp"
                android:background="@drawable/roundedbutton"
                android:layout_marginLeft="40dp"
                android:layout_marginRight="40dp" android:textStyle="bold|italic"/>


        <Button
                android:id="@+id/topTenBtn"
                android:layout_width="match_parent"
                android:layout_height="66dp"
                android:text="@string/top_10"
                android:layout_marginTop="15dp"
                android:background="@drawable/roundedbutton"
                android:layout_marginLeft="40dp"
                android:layout_marginRight="40dp" android:textStyle="bold|italic"/>


        <Button
                android:id="@+id/exitBtn"
                android:layout_width="match_parent"
                android:layout_height="66dp"
                android:text="@string/exit"
                android:layout_marginTop="15dp"
                android:background="@drawable/roundedbutton"
                android:layout_marginLeft="40dp"
                android:layout_marginRight="40dp" android:textStyle="bold|italic"/>
    </LinearLayout>
</FrameLayout>